[pytest]
markers =
    requires_db: marca tests que requieren analytical_db.duckdb real (se omiten si no existe)
    requires_ollama: marca tests que requieren un servidor Ollama real corriendo (no mockeado)
testpaths = ../tests
python_files = test_*.py
pythonpath = ..
